### v0.7.3 (xxxx-xx-xx)

- Bug-fix: `selectbox` now can be initialized with an heterogeneous list of
values, and its value(s) will assume the value of the selected option(s).
- Change: `button` now throws when given invalid arguments.
- Change: `selectbox` now throws when given invalid arguments.
- Internal: tests for `button` and `selectbox`.
